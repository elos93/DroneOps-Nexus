# DroneOps Nexus Frontend

React dashboard for fleet operations, live map visibility and weather-aware dispatch decisions.

## Development

```bash
npm install
npm run dev
```

The local development server proxies `/api` calls to `http://localhost:3100`.
