{
  "name": "droneops-nexus",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -n API,WEB -c cyan,green \"npm run start:dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "test": "npm run test --prefix backend -- --runInBand",
    "lint": "npm run lint --prefix backend && npm run lint --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
